import type * as types from '@/common/types'
import { ROLE } from '@/common/types'

// TODO: Replace with Zustand store when user API is available
export const CURRENT_USER_ROLE: types.Role = ROLE.BRAND
