import ImageUploader, {
  type ImageUploaderProps,
} from "../ImageUploader/ImageUploader"

export default function FormImageUploader(props: ImageUploaderProps) {
  return <ImageUploader {...props} />
}
