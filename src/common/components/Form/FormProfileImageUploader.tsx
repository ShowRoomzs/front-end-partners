import ProfileImageUploader, {
  type ProfileImageUploaderProps,
} from "../ImageUploader/ProfileImageUploader"

export default function FormProfileImageUploader(
  props: ProfileImageUploaderProps
) {
  return <ProfileImageUploader {...props} />
}
